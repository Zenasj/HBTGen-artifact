def average_of_list(lst):
    """
    Calculates the average of all the elements in a list and returns it.

    Args:
        lst (list): A list of numbers to calculate the average for.

    Returns:
        float: The calculated average value, which is a float number in this case.
    """
    return sum(lst) / len(lst)